
#include <stdio.h>

int main()
{
	uint32_t *array;

		// Take a string as input and pass it to htoi()
		while ( (array = GetLine()) )
			decimal = htoi(array);

	printf("The corresponding integer value of %s is %d\n", user_input, decimal);
		
}


// Function returns the address of an array
// Returns a 0 if no number is entered by user
uint32_t htoi(char arr[])
{

}
